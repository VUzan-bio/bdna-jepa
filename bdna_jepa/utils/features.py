"""Sequence feature extraction utilities.

Functions:
    compute_gc_content(sequence)   — GC fraction for a DNA sequence
    compute_kmer_freq(seq, k)      — k-mer frequency vector
    compute_complexity(sequence)   — Linguistic complexity score

← src/cas12a/features.py
"""
# TODO: Port from src/cas12a/features.py
