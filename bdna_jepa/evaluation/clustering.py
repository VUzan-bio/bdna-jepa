"""Clustering evaluation: silhouette, ARI, NMI.

Functions:
    compute_clustering_metrics(embeddings, labels, n_clusters)
        → {"silhouette": float, "ari": float, "nmi": float}

← scripts/04_multilevel_eval.py (clustering part)
"""
# TODO: Port clustering metrics from 04_multilevel_eval.py
